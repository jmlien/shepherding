# Template for creating mp files via shell script using search & replace

# Note: This is not done and should have <graph> and <graph-params>
#graph=(fuzzy n=5000, k=15, simsteps=<simsteps>, tree=(dbrmRRT k=75 bias=1))
#graph=(<graph> <graph-params>)
<mp>
LP=(<lp> <lp-params>)
DM=<dm>
WKSpace=<sh-file>
seed=<seed>
